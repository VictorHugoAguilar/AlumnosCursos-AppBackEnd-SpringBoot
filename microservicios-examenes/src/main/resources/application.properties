spring.application.name=microservicio-examenes
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${random.value}
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Configuracion los tiempo de conexion
#hystrix.command.default.execution.timeout.enabled=false
#hystrix.command.default.execution.isolation.thread.interruptOnTimeout=false
#zuul.host.socket-timeout-millis=60000
#zuul.host.connect-timeout-millis=60000

ribbon.ConnectTimeout=60000
ribbon.ReadTimeout=60000
hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds=60000
zuul.host.socket-timeout-millis=60000
zuul.host.connect-timeout-millis=60000

# Conexion a BD
spring.datasource.url=jdbc:mysql://localhost:3306/db_microservicios_examenes?serverTimezone=Europe/Madrid&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=victor1985
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.open-in-view=true
#spring.datasource.initialize=true
logging.level.org.hibernate.SQL=debug
